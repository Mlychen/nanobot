[project]
name = "timeline-memory-skill"
version = "0.1.0"
description = "Standalone timeline-memory skill bundle"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.2,<9.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
